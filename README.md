# Hehe-circle-vibe-coded-
HIGHLY unserious Project got very bored
try to replace your models attention with it see what happens (I haven't really tested it enough) 

EDIT: probably most useful for long contexts(that the model was not trained on) and anything requiring iterative refinement and real time inputs (cache only works on next step not current so it needs to do things multi step to be actually effective) 


remember to save the weights from the random init it makes training more stable and prevents degradation randomizing every time just confuses the model


might actually serve best in image stacking AIs for photography (I honestly don't know what this is good for someone please test it) 
